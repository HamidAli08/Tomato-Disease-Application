name: tomato_disease_detector
description: Tomato Leaf Disease Detection - FYP Project

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.12.1
  image_picker: ^1.0.7
  image: ^4.0.0
  permission_handler: ^11.3.0
  percent_indicator: ^4.2.3
  cupertino_icons: ^1.0.6

dev_dependencies:
    flutter_test:
      sdk: flutter
    flutter_lints: ^3.0.0

flutter:
   uses-material-design: true
   assets:
     - assets/models/
     - assets/images/
